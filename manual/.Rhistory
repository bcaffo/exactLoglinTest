fortune()
fortune()
fortune()
fortune()
fortune()
fortune()
fortune()
fortune()
fortune()
fortune()
fortune()
fortune()
fortune()
install.packages("exactLoglinTest")
y
ls
library(tools)
residence
ls()
residence.dat
Sweave("/home/bcaffo/tobackup/research/exactLoglinTest/manual/exactLoglinTest.Rnw")
resid.mcx
more print.bab
print.bab
alligator.mcx <- mcexact(y ~ (lake + gender + size) * food + lake * gender * size,
                         data = alligator.dat,
                         nosim = 10 ^ 3,
                         method = "cab", 
                         savechain = TRUE,
                         batchsize = 100,
                         p = .4)

summary(alligator.mcx)
alligator.mcx <- mcexact(y ~ (lake + gender + size) * food + lake * gender * size,
                         data = alligator.dat,
                         nosim = 10 ^ 3,
                         method = "cab", 
                         savechain = TRUE,
                         batchsize = 100,
                         p = .6)

alligator.mcx <- mcexact(y ~ (lake + gender + size) * food + lake * gender * size,
                         data = alligator.dat,
                         nosim = 10 ^ 3,
                         method = "cab", 
                         savechain = TRUE,
                         batchsize = 100,
                         p = .75)

summary(alligator.mcx)
?update.cab
q()
n
